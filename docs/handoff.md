# 引継ぎメモ（Handoff）

最終更新: 2026-03-05

## 現在の進捗

**フェーズ1〜7 すべて完了。** デスクトップアプリとして動作する状態。

| フェーズ | 内容 | 状態 |
|---|---|---|
| 1 | プロジェクト基盤構築 | ✅ 完了 |
| 2 | 機材マスタ管理 | ✅ 完了 |
| 3 | メイン画面（検索・レンタル・返却） | ✅ 完了 |
| 4 | E2Eテスト・仕上げ | ✅ 完了 |
| 5 | PyWebView移行（デスクトップアプリ化） | ✅ 完了 |
| 6 | コード品質改善 & UI微調整 | ✅ 完了 |
| 7 | .exe 化 & 配布準備 | ✅ 完了 |

## アーキテクチャ変更の経緯

v1（フェーズ1〜4）は Flask + ブラウザ構成で、`起動.vbs` → `pythonw.exe` → ハートビート監視という回避策が重なっていた。  
v2（フェーズ5〜7）で PyWebView によるデスクトップアプリ化に移行し、以下を実現：

- `.exe` ダブルクリックで最大化状態で起動
- ウィンドウ × で即終了（ハートビート不要）
- 配布先PCにPython不要

## 未検証事項

- **別PCでの動作確認**: `.exe` + `data/` フォルダを別PCにコピーして動作するかの確認

## 既知の制限事項

- `data/` フォルダは `.exe` と同じディレクトリ階層に配置する必要がある
- 初回起動時に `data/rental.db` が存在しない場合は自動生成される
- Excelインポートは `.xlsx` 形式のみ対応（`.xls` / `.csv` は非対応）

## 次に着手可能なタスク（提案）

特に要件として挙がっているものはないが、改善候補としては以下がある：

1. **UIデザインの改善** — 現在の UI はシンプルで実用的だがやや素朴。モダンな外観への刷新
2. **商品コードのバーコード読み取り対応** — USB バーコードリーダーからの入力に最適化
3. **レンタル期限切れアラート** — 返却予定日を過ぎた機材の自動表示
4. **一括返却機能** — 複数機材をまとめて返却処理

## 主要ファイル

| ファイル | 役割 |
|---|---|
| `src/app.py` | Flask + PyWebView アプリケーション本体 |
| `src/database.py` | SQLite DB操作（CRUD） |
| `src/templates/index.html` | メイン画面HTML |
| `src/templates/equipment.html` | 機材マスタ画面HTML |
| `src/static/main.js` | メイン画面JS（検索・レンタル・返却） |
| `src/static/equipment.js` | 機材マスタ画面JS（一覧・インポート） |
| `src/static/style.css` | スタイルシート |
| `build.spec` | PyInstaller ビルド設定 |
| `docs/requirements.md` | 要件定義 |
| `docs/task.md` | タスク計画 |
| `docs/handoff.md` | 引継ぎメモ（このファイル） |
| `docs/README.md` | 利用者向けドキュメント |
